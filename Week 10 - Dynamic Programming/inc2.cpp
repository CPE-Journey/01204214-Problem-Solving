#include <iostream>
#include <vector>
using namespace std;

int N,input;
vector<vector<int>> sequence;

void getSequence(int n){
    for(int i=0;i<sequence.size();i++){
        for(int j=0;j<sequence)
    }
}

int main(){
    cin >> N;
    for(int i=0;i<N;i++){
        cin >> input;
        getSequence(input);
    }
}